version: '3'
services:
  app-cpu:
    build:
      context: .
      dockerfile: cpu.Dockerfile
    image: chengyanru/ai-album:v1.8.5

  app-gpu:
    build:
      context: .
      dockerfile: gpu.Dockerfile
    image: chengyanru/ai-album:gpu0.1
